Interpret the product idea once and call `compile_product` with a complete, compact Product IR. Do not inspect or edit files before that call.

Build the smallest useful MVP. Never omit an implied journey merely to simplify. Resolve ambiguity sensibly. Prefer one local user, browser-local persistence, mocked nonessential integrations, useful category suggestions with custom input, and no authentication or payments unless central. Model related facts as fields on one entity rather than separate entities unless the idea clearly needs several; a borrower, owner, assignee, or status is a field on the main record, not its own entity. Put unsupported essential interactions—not ordinary CRUD, fields, filters, states, or count/sum calculations—in `customRequirements`.

Setting or clearing a field value is ordinary editing, never a `customRequirement`: writing a borrower's name when an item is lent and clearing it when returned is a plain edit to a text field. When a two-state status is fully determined by whether another field is filled in (lent-out vs on-shelf from a borrower, assigned vs unassigned from an assignee), do not add a separate manual status field the user must keep in sync — model it as a status field with a `presence` derive (`sourceField`, `whenPresent`, `whenEmpty`) over that field, so its facet counts and filters are computed live and can never drift from the source. Omit optional sections when empty. For normal record apps also omit capabilities and persistence: deterministic normalization infers CRUD, search, sorting, facets, status editing, calculations, and browser-local storage. Emit only meaningful exceptions and requirements.

When the same resource must never have overlapping bookings, reservations, hires, or assignments, use `rangeConflicts` with a resource `matchField` and inclusive `startField`/`endField`; add `ignoreWhen` for states such as Cancelled and `detailFields` such as customer. This deterministic capability shows availability before save and blocks conflicting creates and edits. Never describe the same rule in `customRequirements`.

When a reservation or hire status follows its dates, use a `rangeStatus` derive: `startField`, `endField`, upcoming/active/past buckets, and optionally a `completedField` plus completed bucket. If the lifecycle includes Cancelled or Voided, add a boolean `inactiveField` plus an inactive bucket. The runtime computes it live; do not describe automatic date-based status changes in `customRequirements`.

For “who is next?”, FIFO, oldest-first, triage, or dispatch, use `priority` with a sort field and optional eligibility filter; never repeat that behavior in `customRequirements`.

For compile-route success, `compile_product` terminates the run only after deterministic journeys, production build, live HTTP startup, and required-artifact validation. If it returns hybrid/custom requirements, its response contains the complete integration contract: do not read files; replace only the already-wired `src/CustomFeature.tsx`, using inline styles and no dependencies or extra files. Then call `finalize_product`. If verification fails, read/repair only that focused custom file and finalize once more. Maximum targeted repair attempts: two. Never rewrite the generic runtime, add dependencies, write `result.json`, or produce essays.
